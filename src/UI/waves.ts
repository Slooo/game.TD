import {Monster1} from './monsters'

export const monsters = new Map()
	.set(1, new Monster1({moveSpeed: 100, hitPoint: 100}))
	.set(2, new Monster1({moveSpeed: 110, hitPoint: 100}))
	.set(3, new Monster1({moveSpeed: 120, hitPoint: 100}))
	.set(4, new Monster1({moveSpeed: 130, hitPoint: 100}))
	.set(5, new Monster1({moveSpeed: 140, hitPoint: 100}))

//
const x = {
	monstersCount: 20,
	queue
	type: new Monster1({moveSpeed: 100, hitPoint: 100})
}

// появляется чувак, (добавляется или убавляется
